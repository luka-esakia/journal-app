*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build
*.apk

# Signing keys never belong in the repository. CI receives the keystore through the
# KEYSTORE_BASE64 secret instead — see README, "Making updates installable".
*.keystore
*.jks
keystore/
